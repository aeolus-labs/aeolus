# Binaries
/aeolus
/dist/
*.exe

# Go build artifacts
*.test
*.out
coverage.txt

# Editor / OS
.DS_Store
.vscode/
.idea/

# Note: internal/dashboard/web/ build output is committed so `go build`
# works without first running the dashboard build.

# Local config and secrets
*.local.yaml
aeolus.yaml
run-aeolus.sh

# Local strategy / context notes (not shipped)
PRODUCT_CONTEXT.md
.env
.env.local
